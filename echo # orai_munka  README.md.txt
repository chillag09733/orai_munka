echo "# orai_munka" >> README.md
  git init
  git add .
  git commit -m "first commit"
  git branch -M main
  git remote add origin https://github.com/chillag09733/orai_munka.git
  git push -u origin main

git checkout -b feature/grid (-b: ágat hoz létre)
git checkout branch_name (képes ágat váltani, de csak akkor ha már létre lett hozva az ág)
